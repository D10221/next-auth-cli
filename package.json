{
  "private": true,
  "workspaces": [
    "next-auth-cli",
    "demo"
  ],
  "husky": {
    "hooks": {}
  },
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "scripts": {
    "build": "yarn workspace next-auth-cli run build",
    "test": "yarn workspace next-auth-cli run test"
  }
}

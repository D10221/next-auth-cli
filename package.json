{
  "private": true,
  "workspaces": [
    "next-auth-cli",
    "demo"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "yarn workspace next-auth-cli precommit"
    }
  },
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "scripts": {
    "build": "yarn workspace next-auth-cli run build",
    "test": "yarn workspace next-auth-cli run test"
  }
}

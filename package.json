{
  "private": true,
  "workspaces": [
    "next-auth-cli",
    "demos/demo-esm",
    "demos/demo-cjs"    
  ],
  "scripts": {
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"    
  }
}

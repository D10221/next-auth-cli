{
  "name": "demo-esm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "module": "index.js",
  "author": "Mr D.",
  "license": "MIT",
  "dependencies": {
    "next-auth-cli": "0.1.0-1"
  },
  "devDependencies": {
    "mocha": "^8.0.1"
  },
  "scripts": {
    "docker": "yarn workspace next-auth-cli docker",
    "docker:help": "yarn workspace next-auth-cli docker:help",
    "docker:up": "yarn workspace next-auth-cli docker:up",
    "docker:down": "yarn workspace next-auth-cli docker:down",
    "start": "rimraf temp && next-auth-cli sync \"./next-auth-config.js\" && sqlite3 temp/nextauth.sqlite \"select name from sqlite_master\"",
    "test": "mocha",
    "build": "echo N/A"
  }
}
